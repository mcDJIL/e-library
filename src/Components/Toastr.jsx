import { ToastContainer } from 'react-toastify';
import 'react-toastify/dist/ReactToastify.css';

// Komponen ini hanya merender ToastContainer
const Toastr = () => {
  return <ToastContainer />;
};

export default Toastr;
